#include <stdio.h>
#include <string.h>

int main() {
    int buf[100];
    int i = 0;

    scanf("%i", buf);

    for(i = 0; i <= strlen(buf); i++){
     
    }

    printf("%s\n", buf);

    return 0;
}